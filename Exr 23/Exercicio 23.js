function Pessoa() {
  let nome = window.prompt("digite seu nome");
  let sobrenome = window.prompt("digite seu sobre-nome");

  nomeCompleto = nome + sobrenome;

  window.alert( " bem vindo " + nomeCompleto);
}
